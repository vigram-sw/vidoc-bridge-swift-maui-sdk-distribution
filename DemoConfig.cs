namespace MyMauiApp;

// Real token goes into DemoConfig.local.cs (gitignored), see README.
// Without that file the demo builds and runs with the placeholder.
internal static partial class DemoConfig
{
    public static string Token = "YOUR_TOKEN";
}
